name: data
description: Common data module
version: 0.0.1
publish_to: none

environment:
  sdk: '>=2.19.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  common:
    path: ../common
  injectable: ^2.1.0
  get_it: ^7.2.0
  shared_preferences: ^2.0.18

dev_dependencies:
  injectable_generator: ^2.1.3
  build_runner:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mocktail: ^0.3.0
